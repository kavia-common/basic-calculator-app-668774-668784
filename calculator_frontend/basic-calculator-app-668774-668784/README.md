# basic-calculator-app-668774-668784

This workspace contains the `calculator_frontend` React app.

Quick start:
1. `cd calculator_frontend`
2. `npm install`
3. `npm start` (opens on http://localhost:3000 by default)

The app is a modern Ocean-themed calculator supporting keyboard and mouse input.
